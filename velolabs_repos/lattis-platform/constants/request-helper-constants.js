'use strict';

const userTypes = {
    user: "user",
    operator: "operator"
};

const headers = {
    authorization: "authorization"
};

module.exports = {
    userTypes: userTypes,
    headers: headers
};
