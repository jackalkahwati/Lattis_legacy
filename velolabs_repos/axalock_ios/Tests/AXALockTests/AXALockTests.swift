import XCTest
@testable import AXALock

final class AXALockTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(AXALock().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
